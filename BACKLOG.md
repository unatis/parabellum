# Parabellum — бэклог

Живой файл. Решения и спека — в [CLAUDE.md](CLAUDE.md), здесь только «что делаем и в каком порядке».

Статусы: `[ ]` не начато · `[~]` в работе · `[x]` готово · `[!]` заблокировано

Правило порядка: **сначала играбельная база на обычной камере, оптика — последним слоем.** Не лезть в оптику, пока не бегает и не стреляет.

---

## Э0 — Окружение

- [x] **E0.1** Visual Studio **2026 (18.10)**, воркоады *Desktop development with C++* + *Game development with C++* + *.NET desktop development*; компоненты *Visual Studio Tools for Unreal Engine*, *HLSL Tools*, **MSVC v143 v14.44-17.14** (обязателен, см. риски)
- [x] **E0.2** Epic Games Launcher → Unreal Engine 5.8.2
- [x] **E0.3** Включить плагины: *Python Editor Script Plugin*, *Editor Scripting Utilities*
- [x] **E0.4** Проверить, что корпоративная сеть не режет загрузку движка/Fab
- [x] **E0.5** `git init` + `.gitignore` для UE (Binaries, Intermediate, Saved, DerivedDataCache)

## Э1 — Скелет проекта

- [x] **E1.1** Создать C++ проект `Parabellum`, Blank, **без** Starter Content, без Raytracing
- [x] **E1.2** Структура модуля: `Source/Parabellum/{Character,Player,Game,Weapons,Vision,Level}` (добавлен `Game/` — GameMode это правила, не игрок). В `Build.cs` нужен `PublicIncludePaths.Add(ModuleDirectory)`, иначе подпапки в include не видны
- [x] **E1.3** `APBLGameMode`, `APBLPlayerController`, `APBLCharacter` — пустые, но с правильным разделением ответственности (сетевая архитектура с первого дня)
- [x] **E1.4** Прописать классы по умолчанию в `DefaultEngine.ini` (текст, не Blueprint) — чтобы игра стартовала без единого .uasset
- [x] **E1.5** Убедиться, что проект собирается из командной строки (`Build.bat`) — это наш цикл проверки

## Э2 — Движение (референс: CS)

- [x] **E2.1** Enhanced Input: Input Actions + Mapping Context. Генерирует `Tools/make_input_assets.py` через `-run=pythonscript` (без открытия редактора, ~1 c). Раскладка — в скрипте, ассеты в `Content/Input/` — производные
- [x] **E2.2** Ходьба/бег/приседание на `CharacterMovementComponent` — WASD, Space, LCtrl, проверено руками
- [x] **E2.3** Тюнинг ощущения (проход 1 принят). **Проба 2026-09-09: пользователь выбрал «утюг»** (тяжёлое инерционное движение, реалистичнее) вместо CS-резкости — `MaxAcceleration=1500`, `BrakingDeceleration=1200` в `DefaultGame.ini`. Присед: камера теперь плавно едет (`CrouchCameraInterpSpeed=12`), а не прыгает. Оценка: «всё ок»
- [x] **E2.4** Мышь: сырой ввод, чувствительность в тех же единицах что и CS (`0.022 * sens`), **без** сглаживания и акселерации. `bEnableLegacyInputScales=False`. Знак по Y подтверждён руками (вверх = вверх). Подобрано руками: CS sens 1 == наш 16 → введён `MouseCountsScale=16` (подобран в два шага) (эмпирика, источник 1/8 в коде не найден; 0.07 из AxisConfig ни при чём — EI читает RawValue). Теперь `MouseSensitivity` численно равен CS. Живой подбор — CVar `pbl.Sens`
- [ ] **E2.5** Прыжок и его влияние на точность (в CS стрельба в прыжке наказывается)
- [x] **E2.6** Все параметры движения — в `UPBLMovementSettings` (DeveloperSettings, правится через `DefaultGame.ini`, секция `[/Script/Parabellum.PBLMovementSettings]`)

## Э3 — Greybox-уровень

- [x] **E3.1** `Tools/make_greybox_level.py` → `/Game/Maps/Greybox`: 33 бокса (пол, периметр, стены, укрытия, ящики, лестница+платформа, рампа), 4 PlayerStart. Запуск `Tools/make_greybox_level.bat`, без редактора
- [x] **E3.2** Масштаб по CS-метрике: коридор 270, укрытие по грудь 110 (стоя видно поверх, присев — спрятался), по пояс 80, ступень 20×30, платформа 200. Проверено ногами
- [x] **E3.3** Sun 10 lux + SkyLight realtime + SkyAtmosphere, всё Movable, `force_no_precomputed_lighting`; PPV unbound, экспозиция Manual, **bias 9.5** (ручная камера f/4 1/60 ISO100 ≈ EV10 против сцены EV≈4; −1 и +6 были чёрными/тёмными). Принято глазами
- [x] **E3.4** Уровень пересоздаётся скриптом (существующий загружается и вычищается — `delete_asset` на текущем уровне ненадёжен). `.umap` в git как производный

## Э4 — Стрельба

- [x] **E4.1** `APBLWeapon`: hitscan, все числа Config в `DefaultGame.ini`. Меш/анимации/звуки пистолета — из Lyra (`Tools/migrate_from_lyra.py`)
- [x] **E4.2** Владелец стреляет локально сразу (анимация, звук, отдача, предсказание патронов) → `Server_Fire(origin, dir)` → сервер валидирует (кулдаун, патроны, дистанция до камеры), трассирует, `ApplyPointDamage` ×4 в голову, `Multicast_HitFX`
- [~] **E4.3** Пока: случайный конус 0.6° + подброс камеры 1.4° с малым случайным yaw. Паттерн и возврат — дальше
- [x] **E4.4** Луч — из центра экрана вдоль камеры. С системой зрения совместимо без `f⁻¹`: центр в композите не пересчитывается (плотность CS), центральный луч совпадает. `f⁻¹` понадобится только для кликов вне центра (гранаты по курсору и т. п.)
- [~] **E4.5** Decal попадания (`M_BulletDecal`, Python), звук выстрела/сухого щелчка/перезарядки из Lyra, анимация затвора. Нет: искры, трейсер, вспышка
- [~] **E4.6** Магазин 20 (Glock 17+... условно), перезарядка R 2.2 с с анимацией. Переключения оружия нет (одно оружие)
- [x] **E4.7** `APBLHUD`: четыре штриха как в CS + патроны, Canvas, без виджетов

## Э5 — Цели

- [~] **E5.1** Манекены: Mixamo Ch15 (`Tools/import_mixamo.py`, 38 анимаций: pistol locomotion, shooter pack, dying, hit reaction, reloading) стоят в арене с idle. Урон уже приходит (`ApplyPointDamage`, голова ×4 по имени кости), но актор-мишень с здоровьем/реакцией ещё не написан
- [ ] **E5.2** Здоровье и смерть
- [ ] **E5.3** Режим тира: респавн мишеней, счётчик попаданий — чтобы было чем мерить «ощущение стрельбы»
- [ ] **E5.4** Мишени по краям обзора (30–80° от центра) — понадобятся для оценки оптики

**Контрольная точка: игра играбельна на обычной камере — пройдена 2026-09-09 (без стрельбы). Решение пользователя: Э6–Э7 раньше Э4–Э5.**

## Э6 — Оптика, слой 1: боковые рендеры

- [x] **E6.1** `UPBLVisionComponent`: два `SceneCaptureComponent2D` на камере, **±60°, FOV 70, квадратные, RT 1.0×** (v4: покрыть надо 35..67°, а шов лежит в зоне почти полной плотности). 77.5/90 из спеки оставляли непокрытые углы (прямоугольный центр покрывает по вертикали меньше вне оси, бока начинались с 32.5°). Проверено скриншотами
- [x] **E6.2** RT RGBA16f, сторона = min(w,h)×0.5 (360 при 720p), `ResizeTarget` при смене окна
- [x] **E6.3** Боковые: `SCS_FinalColorHDR` (с экспозицией — композит делит на `EyeAdaptationLookup()`), Lumen выкл, TAA вкл (в захватах, похоже, не работает — алиасинг глушится мипами), ShowFlags off: SSR, AO, MB, bloom, flares, vignette, grain. Debug 5 (карта отношения яркости бок/центр) подтвердил совпадение экспозиции в пределах ~3%
- [ ] **E6.4** Зафиксировать экспозицию одинаково во всех трёх рендерах
- [ ] **E6.5** Замерить стоимость. Если Lumen в SceneCapture капризничает — уронить бока на SSGI
- [x] **E6.6** `pbl.VisionDebug 1/2` — боковые RT сырые; `3` — только центр; `4` — композит с тонировкой источников (лево красное, право зелёное); `pbl.Vision 0/1`

## Э7 — Оптика, слой 2: композит

- [x] **E7.1** `Tools/make_vision_material.py` собирает `M_FovealComposite` (SceneColorBeforeBloom) из `Shaders/FovealComposite.ush`. **Ловушки**: VectorParameter в Custom = float3 (без .w) → только скаляры; ошибки компиляции видны лишь при реальном RHI (`Saved/ShaderDebugInfo`), commandlet молчит
- [x] **E7.2** По |yaw|: центр до 35°, смесь до 50°, дальше бок; у краёв кадров веса затухают (8% поля); где источника нет — второй целиком
- [x] **E7.3** Проекция **v4 Панини**. v4.1: ось привязана к вертикали мира (при наклоне головы горизонт прямой; боковые камеры поворачиваются в той же системе), плотность центра = CS fov 90 **при реальном аспекте** (16:9 → 106×74; прежняя калибровка была на 30% плотнее и резала вертикаль до 60°), `d` вычисляется из TotalFOV бисекцией. Дефолт 160° (d=2.16); 180° (d=2.97) выглядит приемлемо — на выбор пользователя. Без зон — без горба. Центральная камера 120° (вертикаль ±44 покрывает композит с запасом)
- [x] **E7.9** Динамическая периферия (CS в покое → шире при повороте) — **проверено и отвергнуто** пользователем 2026-09-14: «при повороте головы картинка плывёт по краям и уезжает за область видимости; до поворота видишь больше, во время — меньше». Изменение проекции во время вращения складывается с самим вращением и читается как сужение. Оставлено опцией `pbl.Vision.Dynamic 1`, по умолчанию выкл; обзор постоянный 140°
- [ ] **E7.10** **«Осознающая периферия»**: края за ~50° — тускло, без цвета, размыто (фовее нечего читать), но движение (разница с предыдущим кадром боков с компенсацией поворота, затухание ~0.3 с) вспыхивает яркостью/контрастом — стимулирует реальную периферию игрока у края монитора. Нужны prev-RT (копия каждый кадр) и параметры дельты поворота в шейдер. Плюс HRTF-звук как второй канал «сбоку»
- [ ] **E7.11** Железо: второй монитор сбоку под углом = настоящая периферия. Проверить окно игры на два экрана (разные размеры мониторов — проблема)
- [ ] **E7.8** (идея, вариант C) Полосы 135–180°: тёмные, обесцвеченные, без деталей, движение подсвечивается (нужен предыдущий кадр боков + компенсация поворота). Периферия как присутствие, а не картинка
- [x] **E7.4** Размытие периферии: мип боковых RT 0.5 у шва → 2.5 у кромки по полному углу от взгляда (дёшево, глушит алиасинг захвата) + 9-тап угловой blur до 0.3°. Едва заметное — по ограничению плоского экрана
- [ ] **E7.5** Опционально: лёгкая десатурация к периферии
- [ ] **E7.6** Bloom — один раз, на композите. AA/motion blur — только внутри рендеров, на композите нет
- [x] **E7.7** Все параметры в `UPBLVisionSettings` (`DefaultGame.ini`), материал получает P0/P1/Debug каждый тик

## Э8 — Депроекция

- [ ] **E8.1** Реализовать `f⁻¹(r)`: экранная точка → направление в мире
- [ ] **E8.2** Подменить вызов из E4.4 — **одна функция на весь проект**: стрельба, гранаты, decals, любой трейс от курсора
- [ ] **E8.3** Тест: выстрел в центр, в 45°, в 85° попадает туда, куда нарисовано
- [ ] **E8.4** HUD/маркеры мира (если появятся) — проецировать той же `f(θ)`, иначе поедут

## Э9 — Тюнинг

- [ ] **E9.1** Профилирование: цель ~1.3–1.5× обычного кадра
- [~] **E9.2** Охота на швы — скриншоты на разных углах и освещении. Известно: вдоль дуги перехода лёгкое затемнение — бока (SceneColorHDR) темнее центра, вероятно Lumen/skylight в захватах. Инструмент готов: `-RenderOffScreen -PBLScreenshotAfter=15 -PBLPlayerStart=PSn -PBLLookYaw=<abs> [-ExecCmds="pbl.VisionDebug 4"]` → `Saved/Screenshots/WindowsEditor/*.png`, агент читает сам
- [ ] **E9.5** Верх/низ экрана при |yaw|<35° берутся из боковых RT (центральный кадр их не покрывает) — там разрешение ~3 px/град против ~20 в центре. Варианты: поднять `SideResolutionScale`, размывать ещё и по pitch, или третий захват вверх/вниз
- [ ] **E9.3** Подбор `f(θ)` по ощущению: стрельба по мишеням в центре и на периферии
- [ ] **E9.4** Переключатель «обычная камера / фовеальная» для сравнения A/B

## Отложено

- Lag compensation / server rewind, dedicated server билд, полировка под пинг
- Арт: Megascans/Fab поверх greybox
- Звуковой дизайн сверх минимума
- Ultrawide (32:9) — на нём система даст более честные 180°, но монитора нет

---

## Риски и заметки

- **.uasset/Blueprint агент не редактирует** — бинарь. Всё, что можно, живёт в C++ и .ini; ассеты, которых не избежать (Input Actions, Render Targets, материалы) — создаются Python-скриптами и лежат в git как генерируемые.
- **Никогда не выключать тик у PlayerController** (`PrimaryActorTick.bCanEverTick = false`). `PlayerTick → ProcessPlayerInput` — это оценка Enhanced Input и поворот камеры. Без тика картина коварная: `InputKey` (F1–F5 debug-бинды) работает, контекст добавляется, маппинги «собраны», таймеры идут — а ни одно действие не срабатывает и мышь мертва. Стоило полдня 2026-09-09. Диагностика, которая нашла: headless-инъекция `InjectInputForAction` + симуляция `InputKey(W)` через таймер.
- **Shift+F1 освобождает курсор только в PIE**, в `-game` его нет; курсор отпускает Alt+Tab. F1–F5 движка убраны через `-DebugExecBindings` в `DefaultInput.ini` (проверить, что подействовало).
- **Greybox-материалы врут**: `WorldGridMaterial` (по умолчанию у движковых кубов) имеет процедурную крапину, зависящую от дистанции и экранных производных — в боковых захватах и у ближнего пола она выглядит как «шов»/шум композита. Пол — ровный серый `MI_Floor`; при охоте на швы сначала смотреть Debug 3 (только центр).
- **Тулчейн**: UE 5.8.2 **не берёт компилятор VS 2026 (v14.51)**, откатывается на **MSVC v143 / v14.44**. Компонент «MSVC v143 - VS 2022 C++ x64/x86 build tools (v14.44-17.14)» обязателен, без него сборки нет.
- **Сборка из CLI работает** (`Build.bat ParabellumEditor Win64 Development`, ~87 c, exit 0) — агент компилирует и читает ошибки сам.
- **Python в UE 5.8 из Python-скрипта**: фабрики зовутся `InputAction_Factory` / `InputMappingContext_Factory`; `unreal.Key()` без аргументов + `set_editor_property('key_name', ...)`; с 5.7 `InputMappingContext.mappings` **deprecated и рантаймом не читается** — `GetMappings()` возвращает `DefaultKeyMappings.Mappings`. Писать `EnhancedActionKeyMapping` в `unreal.InputMappingContextMappingData().mappings` и класть в `default_key_mappings`. Симптом ошибки: контекст добавлен, «маппингов: 0», ввод мёртв.
- **Headless smoke-test работает**: `UnrealEditor-Cmd.exe Parabellum.uproject <map> -game -unattended -nullrhi -nosound -ExecCmds="quit"` → лог в `Saved/Logs/Parabellum.log`. Агент видит, какой GameMode загрузился, что заспавнилось, и все ошибки/варнинги. Не использовать `-abslog` с путём через `~1` — молча не пишется.
- **Цикл проверки**: агент не видит вьюпорт. Нужно решить, как смотрим результат — билд из командной строки + запуск с `-ExecCmds` и скриншотом, или пользователь смотрит и описывает. Это надо наладить на Э1, иначе Э7 (подбор на глаз) встанет.
- **Материал композита** — это .uasset. Скорее всего придётся собирать его Python-скриптом с Custom-нодой, куда подставляется HLSL из .usf файла. HLSL тогда живёт текстом в репозитории и правится нормально.
- **Мышечная память CS не переносится** на периферию — принято как дизайн-решение, не как баг.

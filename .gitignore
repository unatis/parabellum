# Unreal Engine
Binaries/
Build/
DerivedDataCache/
Intermediate/
Saved/
.vs/
*.VC.db
*.opensdf
*.opendb
*.sdf
*.sln
*.suo
*.xcodeproj
*.xcworkspace

# Unreal generated project files
*.vcxproj
*.vcxproj.filters
*.vcxproj.user

# OS
Thumbs.db
desktop.ini

# Source assets for import (FBX, zips) - large binaries, kept locally.
# Что из этого и откуда восстанавливать - в SETUP.md, раздел «Чего нет в репозитории».
# Контуры, снятые с фотографий, - исключение: они лежат в Reference/Contours/ и переносятся
# с проектом, потому что это уже результат обмера, а не исходник.
Import/

# Playwright MCP scratch
.playwright-mcp/

# Локальные исходники и вспомогательное (не в репозитории):
# Import/ - скачанные FBX/архивы/звуки (лицензии смотреть на страницах источников)

# Megascans с Fab: Standard License не разрешает распространение исходных файлов,
# поэтому архивы и производные текстуры в репозиторий не кладём. Скрипты остаются:
# Tools/megascans_prepare.py разложит архивы заново после скачивания с Fab.
Import/Surfaces/megascans/
Import/Surfaces/Concrete_*.jpg
Content/Surfaces/Concrete_*.uasset
